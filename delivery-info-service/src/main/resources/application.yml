server:
  port: 8888

spring:
  profiles:
    active: production
  mongodb:
    embedded:
      version: 3.5.5

docker:
  image:
    version: -1
hello:
  world: Hello World! - delivery-info-service v.${docker.image.version}

